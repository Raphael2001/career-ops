# career-ops monitor — self-contained ignore (does not touch core root .gitignore)
/node_modules
/.next*
/out
/dist
/.turbo
*.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store

# Yarn (node-modules linker) -- yarn.lock and .yarnrc.yml ARE tracked, this
# is just the local install-state cache.
.yarn/install-state.gz
